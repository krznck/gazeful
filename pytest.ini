[pytest]
testpaths = tests
python_files = test_*.py *_test.py
pythonpath = .
markers = 
	visual: for visual outputs (not automatic testing)
	perf: for performance testing
addopts = -m "not visual and not perf"
